spring:
  datasource:
    url: jdbc:mysql://mysql-svc:3306/employee
    username: test
    password: testuser
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # You might want to change this based on your requirements (e.g., validate, create, update, none)
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
    generate-ddl: true
  application:
    name: springboot-backend
server:
  port: 8080 # Change this if needed

